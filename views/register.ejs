<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <title>Register</title>
</head>
<body>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top" id="mainNav">
        <div class="container">
          <a class="navbar-brand js-scroll-trigger" href="#page-top" style="color: white;">AttendanceApp</a>
        </div>
      </nav>   
<br>

<div class="container white">
    <br>
    
    <div class="container">
        
            <div class="card card-signin my-5">
                <div class="card-body">
                    <h3 class="black-text center">Register</h3>
                    <form autocomplete="off" action="/users/register" method="POST">
                <div class="form-row">
                    <div class="col">
                        <label for="eid">Employee ID</label>
                        <input class="form-control" name="eid" type="text" required <% if(typeof prevInput != 'undefined') { %> 
                            value = "<%= prevInput.eid %>" <% } %> >
                    </div>
                    <div class="col">
                        <label for="name">Name</label>
                        <input class='form-control' name="name" type="text" required <% if(typeof prevInput != 'undefined') { %>
                            value = "<%= prevInput.name %>" <% } %> > 
                    </div>
                </div>
                    <div class="form-group"  style="margin-top:0;">
                        <label for="email">Email</label>
                        <input class="form-control" name="email" type="email" class="validate" required <% if(typeof prevInput != 'undefined') { %>
                            value = "<%= prevInput.email %>" <% } %> >
                    </div>
                
                    <div class="form-row">
                    <div class="col">
                        <label for="password">Password</label>
                        <input class="form-control" name="password" type="password" class="validate" required <% if(typeof prevInput != 'undefined') { %>
                            value="<%= prevInput.password %>" <% } %> >
                    </div>
                
                
                    <div class="col">
                        <label for="password">Confirm Password</label>
                        <input class="form-control" name="password2" type="password" class="validate" required <% if(typeof prevInput != 'undefined') { %>
                            value="<%= prevInput.password2 %>"<% } %> >
                        
                    </div>
                </div>
                <button type="submit" class="btn btn-primary">Register</button>
                <br>
                <br>
            </form>
        </div>
        <% if(typeof errors != 'undefined') { %>
        <div class="error-msgs">
            <p class="alert alert-danger"><%= errors[0].msg %></p>
        </div>
        <% } %>
        </div>
        </div>
        </div>
    </div>
</div>
</body>
</html>